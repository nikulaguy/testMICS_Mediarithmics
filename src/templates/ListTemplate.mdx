import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ListStories from './ListTemplate.stories';
import { DocTable } from '../docs/DocTable';
import { DocMeta } from '../docs/DocMeta';

<Meta of={ListStories} />

# Template — Liste

<DocMeta>
  <span><b>Catégorie</b> : template</span>
  <span><b>Source code</b> : `src/templates/ListTemplate.tsx`</span>
  <span><b>Référence</b> : l'écran Segments, dont il est extrait</span>
</DocMeta>

Coque commune à tous les écrans de liste. Elle **ne décide de rien** : ni des colonnes, ni des
filtres, ni de la donnée. Elle fixe uniquement ce qui doit être identique d'un écran à l'autre —
l'ordre des blocs, la surface, et les espacements entre eux.

<Canvas of={ListStories.Complet} />

## Props

<Controls of={ListStories.Complet} />

## Ce qu'il garantit

<DocTable
  headers={['Bloc', 'Règle tenue par le template']}
  rows={[
    ['1 · Barre d\'outils', "Recherche à gauche, actions à droite, sur une seule rangée. Elle disparaît entièrement si l'écran n'a ni recherche ni action."],
    ['2 · Filtres actifs', "Toujours SOUS la barre d'outils, jamais au-dessus. Absente — et non vide — quand aucun filtre n'est appliqué : pas de hauteur réservée."],
    ['3 · Contenu', "Le tableau, ou l'état vide. C'est le seul bloc que la page fournit obligatoirement."],
    ['4 · Pagination', <>Sous le contenu, alignée à droite. <b>Fait partie du template</b> : la page fournit le composant <code>Pagination</code>, le template fixe sa place et son alignement.</>],
  ]}
/>

## Ce qu'il ne fait pas

Le **fil d'ariane** et les **actions de page** (Export, New…) n'y sont pas. Ils vivent dans
l'Actionbar, qui appartient à la coque de l'application et non à la liste : un écran de détail les
porte aussi, sans être une liste.

La **pagination**, elle, appartient bien au template : c'est lui qui garantit qu'elle est sous le
contenu et alignée à droite, sur tous les écrans de liste. La page se contente de lui passer le
composant `Pagination` renseigné — le nombre de pages dépend du filtrage, que le template ignore.

<Canvas of={ListStories.AvecFiltresActifs} />

## Exposé ou panneau : la règle des trois dimensions

Décision de la revue client du 1er septembre 2026. Jusqu'à **trois dimensions de filtrage**,
chaque filtre a son déclencheur dans la barre d'outils (modèle **exposé** — Campaigns : label,
période, statut). Au-delà, un seul bouton **Filters** ouvre le panneau en cascade (modèle
**panneau** — Segments). Jamais les deux à la fois, et **plus de filtre dans les en-têtes de
colonnes** : il doublait une dimension du panneau pour un usage marginal, une dimension a une
seule porte d'entrée.

Sur le modèle exposé, le déclencheur d'une multi-sélection porte le **nom de la dimension, jamais
sa valeur** : « Status », « Label », quel que soit l'état. Ce qui est coché se lit dans le panneau
ouvert et dans la barre de rappel — voir la page ActiveFilterBar.

## Les deux actions de barre d'outils

Tout écran de liste porte les mêmes deux actions à droite : **Filters** et **Edit view**. Le
template ne les fournit pas — il fournit leur **place**. C'est volontaire : les dimensions de
filtrage et les colonnes disponibles changent à chaque écran, les imposer ici reviendrait à
recoder Segments dans le template.

**Toutes les stories de cette page sont réellement manipulables.** Filters ouvre la vraie cascade à
deux niveaux du `FilterPanel` : on choisit une dimension à gauche, on coche ses valeurs à droite, le
compteur du bouton s'incrémente, la chip apparaît et le tableau se filtre. Edit view retire une
colonne. Les deux panneaux se ferment au clic extérieur et à Échap.

<DocTable
  headers={['', 'Filters', 'Edit view']}
  rows={[
    ['Icône', <code>filter</code>, <code>view</code>],
    ['Contenu', 'Le FilterPanel en cascade, ou une liste de cases quand il y a peu de dimensions', 'Une case à cocher par colonne disponible'],
    ['Compteur', <>Un <code>CountBadge</code> portant le nombre de <b>filtres</b>, pas de dimensions</>, 'Aucun'],
    ['Pied', '« Clear all filters », icône balai', 'Aucun'],
    ['Application', 'Immédiate, sans bouton de validation', 'Immédiate'],
  ]}
/>

Trois points de montage à respecter, visibles dans le code de la story :

- **Chaque déclencheur porte son panneau**, en position absolue dans un parent `position: relative`.
  Le template ne connaît pas ces panneaux, il ne les positionne pas.
- **Ouvrir l'un ferme l'autre.** Deux panneaux ouverts en même temps se recouvrent, et le second
  clic devient imprévisible.
- **`aria-haspopup` et `aria-expanded` sur le bouton**, sinon rien n'annonce qu'il ouvre quelque
  chose.

Le compteur compte les **filtres**, jamais les dimensions : trois types cochés dans « Segment
type », c'est 3 — le même nombre que les chips affichées juste en dessous. Compter les dimensions
donnerait 1, et le badge contredirait la barre.

## Règles d'usage

- **Un écran de liste passe par ce template.** Recomposer la surface à la main, c'est réintroduire
  l'écart que le template existe pour supprimer — un padding ici, un gap là.
- **L'état vide remplace le tableau, pas la page.** La barre d'outils et les chips restent
  affichées : c'est ce qui permet de comprendre *pourquoi* il n'y a rien. Un écran qui se vide
  entièrement laisse l'utilisateur sans moyen d'annuler ce qu'il vient de faire.
- **Les chips ne portent que ce que la barre d'outils ne montre plus.** Les multi-sélections y
  vont toujours ; un filtre à valeur unique lisible dans son sélecteur n'a pas besoin d'une chip,
  il en aurait deux représentations concurrentes.
- **Pas de second niveau de surface.** Le contenu est posé directement dans la carte du template :
  l'envelopper dans une `Card` doublerait le padding.

<Canvas of={ListStories.SansResultat} />

## Écrans qui l'utilisent

`Campaigns` aujourd'hui — c'est aussi l'écran de référence du modèle exposé : trois dimensions,
dont deux multi-sélections (Label, Status) rappelées en chips. `Segments`, dont il est extrait,
porte encore sa propre coque : la migration est mécanique mais touche un écran très instrumenté,
elle est faite à part.

<Canvas of={ListStories.Minimal} />

## Accessibilité (RGAA)

<DocTable
  headers={['Thème', 'Exigence']}
  rows={[
    [
      'Structuration',
      <>
        Le template rend une <code>&lt;section&gt;</code>. Le titre de l'écran vit dans l'Actionbar,
        au-dessus : la liste n'est pas un document à elle seule (RGAA 9.1).
      </>,
    ],
    [
      'Ordre de tabulation',
      "L'ordre du DOM suit l'ordre visuel — recherche, actions, chips, tableau, pagination. Aucun bloc n'est déplacé en CSS (RGAA 12.8).",
    ],
    [
      'Contenu dynamique',
      "Le nombre de résultats après filtrage est annoncé par l'écran, pas par le template : lui ne connaît pas la donnée.",
    ],
  ]}
/>

## Écarts et évolutions

- **À construire.** Un template `Détail` sur le même principe, extrait de la page de détail d'un
  segment.
- **À construire.** La migration de l'écran Segments, aujourd'hui seul écran de liste à ne pas
  passer par le template.
